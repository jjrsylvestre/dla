##DESCRIPTION
##  
##ENDDESCRIPTION

## DBsubject('Linear Algebra')
## DBchapter('AUMAT220 Ch 8')
## DBsection('AUMAT220 S 3')
## Date('3/5/11')
## Author('J. Sylvestre')
## Institution('University of Alberta, Augustana Faculty')
## TitleText1('')
## EditionText1('')
## AuthorText1('')
## Section1('')
## Problem1('')

DOCUMENT();      
loadMacros("PGbasicmacros.pl",
           "PGchoicemacros.pl",
           "PGanswermacros.pl",

);
TEXT(beginproblem(), $BR,$BBOLD, "Inverse Linear Transformations", $EBOLD, $BR,$BR);

# True false questions are a special case of a "select list"
# Make a new select list
$tf = new_select_list();
# $tf now "contains" the select list object.
# Insert some  questions and whether or not they are true.

$tf -> qa ( # each entry has to end with a comma
"If \(T\colon V \to W\) is a one-to-one linear transformation, then \(\ker(T)\) is the empty set.",
"F",
"If \(\dim V = \dim W\) and \(T\colon V \to W\) is a linear transformation, then \(T\) is one-to-one if and only if it is onto.",
"T",
"If \(T\colon V \to W\) is a one-to-one linear transformation, then there exists an linear transformation \(T^{-1}\colon W \to V\) such that \(T^{-1}\bigl(T(\mathbf{v})\bigr) = \mathbf{v}\) for all \(\mathbf{v}\in V\) and \(T\bigl(T^{-1}(\mathbf{w})\bigr) = \mathbf{w}\) for all \(\mathbf{w}\in W\).",
"F",
"If \(T\colon V \to V\) is a one-to-one linear operator, then \((T\circ T)^{-1} = T^{-1}\circ T^{-1}\).",
"T",
"The linear transformation \(T \colon \mathbb{M}_{22} \to \mathbb{P}_3\) defined by \[ T\left( \begin{bmatrix} a & b \\ c & d \end{bmatrix} \right) = a + bx + cx^2 + dx^3\] is both one-to-one and onto.",
"T",
);   

# Choose five of the question and answer pairs at random.
$tf ->choose(5);

# Now print the text using $ml->print_q for the questions
# and $ml->print_a to print the answers.

BEGIN_TEXT
$PAR

Enter T or F depending on whether the statement is true or false.
(You must enter T or F -- True and False will not work.)$BR

$PAR

$BBOLD In the following, assume that \(V\) and \(W\) are finite dimensional abstract vector spaces, unless otherwise stated. $EBOLD

$PAR

\{ $tf-> print_q \}

$PAR

END_TEXT

# Enter the correct answers to be checked against the answers to the students.

install_problem_grader(~~&std_problem_grader);
# Since this is a true questions, we do not usually wish to tell students which
# parts of the matching question have been answered correctly and which are
# incorrect.  That is too easy.  To accomplish this we set the following flag to
# zero.
$showPartialCorrectAnswers = 0;

ANS(str_cmp( $tf->ra_correct_ans )   ) ;

ENDDOCUMENT();        # This should be the last executable line in the problem.
